{
  "dependencies": {
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "http": "^0.0.1-security",
    "https": "^1.0.0",
    "mongoose": "^6.0.7",
    "socket.io": "^4.2.0",
    "socket.io-client": "^4.2.0"
  },
  "scripts": {
    "build": "rm -rf build && cd ../react-app/ && npm run build --prod && cp -r build ../express-app"
  }
}
